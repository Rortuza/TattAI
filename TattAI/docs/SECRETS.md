# Secrets handling

This project reads `secrets.properties` from Android resources.

Create:
`app/src/main/resources/secrets.properties`

Example:
```properties
OPENAI_API_KEY=...
OPENAI_MODEL=gpt-4o-mini
FIREBASE_ENABLED=false
```

This file path is gitignored in this repo template.
